# QUEST LOG 重构任务

## 一、你的项目

**项目名称：** QUEST LOG
**项目位置：** C:\Users\MMCGA\Desktop\Quest-log
**项目类型：** 个人任务管理工具（FastAPI + SQLite + 原生前端）

**当前状态：** 功能完整，能跑，但视觉效果、交互体验、整体质感都达不到你满意。


## 二、你的核心动机

**你为什么做这个项目：**
“执行任务本身不痛苦，但决策非常痛苦且困难。我需要一个外部工具帮我做好决策、拆解任务，把任务移出工作记忆，让我能专注在当下的单个任务上。”

**你的驱动力：**
“工具要好看、要有反馈、要有获得感。每次打开都觉得舒服，每次点击都有响应，每次完成都有仪式感。”


## 三、你的核心原则

1. **好看是最高优先级。** 视觉先于功能。不好看的功能可以暂时去掉或重做。
2. **每次交互都有物理反馈。** 点击有下沉回弹，悬停有浮起和阴影变化，切换有平滑过渡。
3. **修改不用保守。** 不要拘泥于现有代码。如果觉得哪个页面应该推倒重来，直接重写。目标是“变得更好看”，不是“改得尽可能少”。
4. **风格对标明日方舟终端界面 + 鹰角官网。** 关键词：磨砂玻璃、工业机能、纸张做旧、克制高饱和、复合材质、光影分层、真金色。
5. **资源可以自由下载。** 什么都可以下载，哪个好看用哪个。纹理、字体、图标、背景图、装饰元素，自行搜索下载或自行设计。优先 MIT/CC0 协议，找不到合适的用 CSS/SVG 生成替代。


## 四、要改的东西

### 4.1 视觉系统（全部重做）

**颜色系统：** 扩展到 60+ 个色值，每个色系 5-10 个变体。不同面板之间允许色温差异。色系包括深色基底、中灰面板、磨砂玻璃、主文字、次要文字、高亮红、信息蓝、点缀金、点缀橙、成功绿、警告黄、紫色。

**材质系统：** 所有卡片和面板实现磨砂玻璃质感：半透明背景 + 背景模糊 + 细边框 + 分层阴影。叠加噪点纹理（透明度 3%-5%），边缘有轻微磨损感。不能是纯色半透明块，要有真实的玻璃和纸张质感。

**光影系统：** 所有元素有完整光影：上方大阴影 + 前上方阴影。交互时有阴影深浅变化，模拟物理世界的“浮起”和“下沉”。

**字体系统：** 标题/数字用 Orbitron，正文用常规无衬线字体。字号层级清晰，字距适当。

### 4.2 交互系统（全部重做）

**按钮：** 重新设计尺寸（更大、更易点击）。所有可点击元素必须有完整三态：默认（立体感）→ 悬停（上浮，阴影增强）→ 按下（下沉，阴影减弱，有压缩感）。每次点击都有物理反馈感。

**卡片：** 多层复合设计（背景层 + 装饰层 + 信息层）。侧边装饰带，参考明日方舟干员卡片。卡片悬停时有浮起效果。

**筛选栏：** 重新布局，让按钮更大、更易用、更美观。

**整体布局：** 如果觉得当前布局不舒服，可以重新设计。

### 4.3 功能系统（新增或重做）

**星星组件：** 所有星星真金色（#E8B818 ~ #F8D840），倾斜 15°。1~6 星区分靠背景板：1 星偏白、2 星黄绿、3 星蓝色冷调、4 星紫白混合有晶体感、5 星白金混合复杂设计、6 星褐色深色晶体 + 绿→黄→橙→橙红色带渐变 + 点状晶体装饰。星星不能溢出容器。

**色条：** 保留但重新设计。改为卡片左侧或右侧的装饰带，作为等级/状态的辅助视觉标识。

**奖励系统：** 完成任务有结算弹窗（经验值、龙门币、随机掉落）。等级进度条实时更新，升级有动画。有随机性（概率掉落额外奖励）。让每次完成都有仪式感和获得感。

**现实奖励兑换（新增）：** 用户可自定义“积分/等级目标 → 兑换现实奖励”的规则。系统提供 CRUD。达到目标时弹窗提醒。只负责提醒和记录，不涉及实际支付或物流。

**番茄钟（新增）：** 自定义专注时长和休息时长（默认 25+5）。专注时沉浸模式：只显示当前任务 + 计时器 + 进度。计时结束提醒切换。

**追踪模式（重做）：** 顶部显示完整父任务链，每一级都可点击跳转。显示当前任务全貌：标题、描述、星级、进度、计时器、子任务清单。像游戏任务追踪器。

**Logo（重做）：** 重新设计一个更精致的 Logo，参考明日方舟/鹰角官网品牌风格。

**图谱（提升视觉）：** 保留基础功能，视觉上重新设计。节点用多层复合图案。

**日历（提升视觉）：** 明确用途：显示截止时间分布，视觉上重新设计。

**移动端：** 桌面端和移动端同步优化。

### 4.4 后端改动

后端需要配合新增功能（番茄钟记录、现实奖励兑换）进行扩展。具体表结构、字段名、API 设计由你根据项目现有风格自行决定。不改动现有 API 逻辑。


## 五、禁止项

- emoji 图标（用 Font Awesome 或 SVG）
- 普通后台管理风格
- AI 生成感
- 单层卡片
- 纯色方块按钮
- 状态指示灯、雷达图标、脉冲光效
- 扁平无阴影设计
- 高饱和色乱用
- 白板背景
- 通用科技感模板


## 六、参考信息

项目根目录下有以下参考文件：
- `ui_spec.md` — 你原来的 UI 视觉规范文档
- `requirement_v1.md` — 你原来的产品需求文档
- `references/` 文件夹 — 你放的明日方舟游戏截图、鹰角官网截图、你自己当前界面截图

执行时参考这些文档和截图，自行判断什么是“明日方舟风格”，什么是“够好看”。


## 七、工作方式

你有完全的修改自由度。可以重写任何前端文件、修改布局、新增功能、删除不合适的设计。不需要请示，直接做。

修改完成后，输出所有修改的文件完整内容。


## 八、额外任务：生成一个给外部 AI 用的 Prompt

在你完成全部重构后，额外生成一份 `PROMPT_FOR_AI.md` 文件，放在项目根目录。

**这个 prompt 的用途：**
用户把它发给任何一个外部 AI（比如 ChatGPT、Claude、DeepSeek 网页版），然后用户说“我要做 XX 事情”，AI 和用户对话商讨，确定好任务细节后，输出一个符合 QUEST LOG 导入规范的 JSON 文件。

**prompt 的内容应包括：**

1. **你是谁：** QUEST LOG 是一个任务管理工具，支持任务、子任务、优先级（1-6 星）、任务线（主线/支线）、状态、截止时间、可计数目标、标签、奖励配置等字段。告诉 AI 这个工具是什么。

2. **你的任务：** 用户会告诉你“我想做 XX 事情”，你需要和用户对话，收集所有必要信息，然后生成一个 JSON 文件，用户可以直接导入 QUEST LOG 执行。

3. **你需要收集什么信息：** 任务名称（必填）、描述、优先级（1-6星）、任务线（主线/支线）、状态、截止时间、是否为可计数任务（目标值）、子任务列表（嵌套）、标签。每一轮问 1-2 个问题，不要一次问完。

4. **输出要求：** 严格按照 QUEST LOG 的导入 JSON 格式输出。格式参考 `requirement_v1.md` 中的第 10 节“面向 AI 的 JSON 导入格式”。

5. **对话风格：** 像游戏里的任务发布员，而不是客服。比如：“博士，新任务收到了。确认一下——这个任务的紧急程度是几星？”而不是“请问您能为该任务分配一个优先级吗？”

6. **输出前确认：** 在输出 JSON 前，向用户展示任务概要，让用户确认无误后再输出 JSON。

**最终输出：** 当用户确认所有信息无误后，只输出纯 JSON 代码块，可直接复制粘贴到 QUEST LOG 的导入窗口中。